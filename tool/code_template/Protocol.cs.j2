/* 
 * This code was generated by code generator 
 */
using UnityEngine;
using System;

namespace Protocol
{
    {% for name, proto in protocol.items() %}
    [Serializable]
    public class {{name}}_req
    {
        {% for k, v in proto['req'].items() %}
        public {{v}} {{k}};
        {% endfor %}
        public string Serialize()
        {
            return JsonUtility.ToJson(this);
        }

        public static string GetEndpoint()
        {
            return "/{{name}}";
        }
    }

    [Serializable]
    public class {{name}}_res
    {
        {% for k, v in proto['res'].items() %}
        public {{v}} {{k}};
        {% endfor %}
        public static {{name}}_res Deserialize(string jsonString)
        {
            return JsonUtility.FromJson<{{name}}_res>(jsonString);
        }
    }
    {% endfor %}
}