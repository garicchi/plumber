#
# This code was generated by code generator
#
import sqlite3

def dict_factory(cursor, row):
    d = {}
    for idx, col in enumerate(cursor.description):
        d[col[0]] = row[idx]
    return d


{% for table in tables %}
class {{table['name']}}:
    {% for column in table['columns'] %}
    {{column['name']}}: {{column['type']}}
    {% endfor %}
    @classmethod
    def select(cls, cond: str='', param: str=''):
        con = sqlite3.connect("/var/masterdata.db")
        con.row_factory = dict_factory
        cur = con.cursor()
        for row in cur.execute(f'SELECT * FROM {{table['name']}} {cond}', param):
            r = cls()
            for k, v in row.items():
                r.__setattr__(k, v)
            yield r
        con.close()

{% endfor %}
